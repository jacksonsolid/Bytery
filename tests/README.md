# Test Layout

The current canonical test and benchmark project remains in the repository root at:

- `Test/`

This `tests/` folder exists as part of the public repository organization and can host future test reorganizations without changing the current stable project layout.
