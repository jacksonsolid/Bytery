# Source Layout

The current canonical VB.NET project remains in the repository root at:

- `Bytery/`

This `src/` folder exists as part of the public repository organization and can host future source reorganizations without changing the current stable project layout.
